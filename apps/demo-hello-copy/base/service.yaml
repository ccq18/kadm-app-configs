apiVersion: v1
kind: Service
metadata:
  name: hello-copy
  namespace: apps
  labels:
    app.kubernetes.io/name: hello-copy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hello-copy
  ports:
    - name: http
      port: 80
      targetPort: http
